      var food = ["ali", "reza", "ahmad"];
      console.log(food);
      food.push("amier");
      console.log(food);
      // Adds new elements to the end of an array, and returns the new length


      var fruits = ["apple", "banana", "mango"];
      console.log(fruits);
      console.log(fruits.length);
      //Sets or returns the number of elements in an array

      var Nature= ['coconut','Pineapple','Tree','Garden'];
      console.log(Nature);
      console.log(Nature.toString());
      //Converts an array to a string, and returns the result

      var Book= ['Novel','story','Report','Poems','favor'];
      console.log(Book);
      console.log(Book.slice(3, 5));
    //	Selects a part of an array, and returns the new array

    const arr1 = ['Cecilie','lone'];
    const arr2 = ['emill','tobias','linus'];
    const children = arr1.concat(arr2);
    // 	Joins arrays and returns an array with the joined arrays

    var fruits = ['coconut','mango','Pineapple','Lemon','banana'];
    fruits.sort();
    console.log(fruits);
    // Sorts the elements of an array

    